# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely need to edit after that.
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'jekyll serve'. If you change this file, please restart the server process.

# Site settings
title: The Ruse Programming Language
description: > # this means to ignore newlines until "baseurl:"
    An embedded Lisp for Rust.
baseurl: "" # the subpath of your site, e.g. /blog
url: "http://www.ruse-lang.org" # the base hostname & protocol for your site

# Build settings
markdown: kramdown

# Make sure to disable plugins, for compatibility with GitHub Pages
safe: true

# Exclude certain files
exclude: ["CNAME", "Gemfile", "Gemfile.lock", "README.md", "LICENSE"]

